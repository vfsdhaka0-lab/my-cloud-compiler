import 'package:flutter/material.dart';

void main() {
  runApp(MaterialApp(
    home: Scaffold(
      appBar: AppBar(title: Text('Róyal Event')),
      body: Center(child: Text('Build Successful!')), 
    ),
  ));
}